{
  "name": "gerenciador-colaboradores",
  "version": "1.0.0",
  "description": "Sistema de gerenciamento de colaboradores para projetos",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "build": "cd client && npm run build",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install"
  },
  "keywords": ["colaboradores", "gestão", "projetos", "react", "node"],
  "author": "Codexion",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}